#include <iostream>

//  Another way to define a constant is
#define Z 10;

int main()
{
    //  Normal variable declaration is a literal constant
    int x = 5;

    //  Symbolic constants 
    const int y = 5;
    //  This is now const qualified, which means prefixed with const

    return 0;
}